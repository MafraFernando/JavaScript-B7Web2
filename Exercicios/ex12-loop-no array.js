for (let n = 0; n < 10; n++) {
    console.log('Item n ' + n);
};

let cores = ['branco', 'preto', 'marrom', 'azul', 'vermelho', 'verde'];

for (let n = 0; cores.length; n++) { //o loop só roda a quantidade de vezes que o array tem
    console.log(cores[n]);
}